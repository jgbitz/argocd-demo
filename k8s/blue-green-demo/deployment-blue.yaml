apiVersion: apps/v1
kind: Deployment
metadata:
  name: bg-demo-blue
  labels:
    app: blue-green-demo
    version: blue
spec:
  replicas: 0
  selector:
    matchLabels:
      app: blue-green-demo
      version: blue
  template:
    metadata:
      labels:
        app: blue-green-demo
        version: blue
    spec:
      containers:
        - name: nginx
          image: nginx:1.27-alpine
          ports:
            - containerPort: 80
          volumeMounts:
            - name: html
              mountPath: /usr/share/nginx/html
      volumes:
        - name: html
          configMap:
            name: blue-index
            items:
              - key: index.html
                path: index.html
